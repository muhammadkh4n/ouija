{
  "name": "ouija",
  "private": true,
  "packageManager": "npm@11.8.0",
  "workspaces": ["packages/*"],
  "scripts": {
    "build": "turbo run build",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "gen:migrations": "node scripts/gen-status-migration.mjs",
    "gen:migrations:check": "node scripts/gen-status-migration.mjs --check",
    "smoke:keychain": "node scripts/smoke/keychain-provider-check.mjs"
  },
  "devDependencies": {
    "turbo": "^2.9.18",
    "typescript": "^6.0.3",
    "vitest": "^3.2.4",
    "@types/node": "^25.6.2"
  }
}
